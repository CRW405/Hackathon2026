Admin client UI

This folder contains a small Flask app that acts as the administrative UI.

How to run:

1. Ensure the backend server (admin/server/server.py) is running on port 6000.
2. From repository root run:

   python admin/client/client.py

The UI will be available at http://127.0.0.1:5000
